# Policy Enforcer for Laravel

A central policy enforcement middleware for Laravel applications.
This package allows you to enforce global or API-level policies from a single place.

---

## 📦 Installation

Install the package via Composer:

```bash
composer require primo/policy-enforcer
